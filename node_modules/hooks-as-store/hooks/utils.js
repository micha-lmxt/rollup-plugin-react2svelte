export const unequalArgs = (oldArgs, newArgs) => {
    return (oldArgs === undefined ||
        newArgs === undefined ||
        oldArgs.length !== newArgs.length ||
        oldArgs.some((v, i) => v !== newArgs?.[i]));
};
