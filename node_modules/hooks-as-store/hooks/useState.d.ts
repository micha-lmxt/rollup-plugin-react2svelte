import { type Writable } from 'svelte/store';
export declare type UseState<T> = Writable<T> & {
    setter: (t: T | ((prev: T) => T)) => void;
};
export declare function useState<T>(start: T | (() => T)): [T, (val: T) => void];
