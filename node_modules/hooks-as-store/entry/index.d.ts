import { type Readable } from 'svelte/store';
interface CustomHook<T extends any[], S> extends Readable<S> {
    run: (...args: T) => void;
}
export declare function hook<T extends any[], S>(hookfunction: (...args: T) => S, ...args: T): CustomHook<T, S>;
export declare function hookGroup(hooks: any[]): CustomHook<[any[]], any[]>;
export {};
